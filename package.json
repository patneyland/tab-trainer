{
  "name": "tab-trainer",
  "version": "0.1.0",
  "private": true,
  "description": "Train your ear, eyes, and voice for intervals — preparing to audition for the Tabernacle Choir.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace packages/core && npm run build --workspace apps/web",
    "build:core": "npm run build --workspace packages/core",
    "test": "npm run test --workspace packages/core"
  },
  "engines": {
    "node": ">=20"
  }
}
